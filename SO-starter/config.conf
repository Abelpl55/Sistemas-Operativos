# config.conf — configuración base del proyecto final
# Este archivo es leído por los scripts con: source ./config.conf

# TODO-00: Revisa si las rutas son correctas para tu equipo.
# Si cambias nombres de carpetas, actualiza estas variables.

PROJECT_NAME="proyecto_final_so"
SOURCE_DIR="./sitio_web"
BACKUP_DIR="./backups"
LOG_DIR="./logs"
REPORT_DIR="./reportes"
EVIDENCE_DIR="./evidencia"

ACCESS_LOG="./logs/access.log"
ERROR_LOG="./logs/error.log"

# Número máximo de respaldos que se deben conservar.
MAX_BACKUPS=5

# Nombre del equipo.
# TODO-00A: Cambia este valor por tu nombre de equipo.
STUDENT_ID="NOMBRE_EQUIPO"

